Remove TOTP/MFA; prepare for Entra ID (Azure AD) auth

- Backend: drop MFA routes and mfaController, User.mfaSecret/mfaEnabled,
  signMfaTempToken; remove speakeasy and qrcode deps
- Shared: auth API now login/getMe/refresh/logout only; remove mfaSetup,
  mfaEnable, mfaVerify, mfaDisable
- Frontend: single-step login (email+password); remove MFA step and
  dashboard "Enable MFA" section; simplify AuthContext (no loginMfa)
- Docs: README, ARCHITECTURE-NOTES, TESTING-REFRESH-AND-MFA updated;
  note MFA will come from IdP (e.g. Microsoft Entra) later
- E2E and unit tests updated; backend and frontend tests pass
