{
  "name": "backend",
  "targets": {
    "test": {
      "command": "node node_modules/vitest/vitest.mjs run",
      "options": { "cwd": "{projectRoot}" }
    },
    "build": {
      "command": "npm exec -- tsc",
      "options": { "cwd": "{projectRoot}" }
    }
  }
}
